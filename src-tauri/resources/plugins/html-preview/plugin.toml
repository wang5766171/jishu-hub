schema = 1
kind = "tool"

[info]
id = "html-preview"
display_name = "HTML 页面预览"

# v0.9.2 测试期：纯闸门形态（无 entry）——preview_html 工具由全局部署的
# extensions/html-preview.ts 扩展注册；本清单仅治理其 spawn --tools 白名单
# 归属（启停热联动，同 interactive-qa 之于 request_user_input）。
[pi_extension]
target_agent = "jishu-self"
tools = ["preview_html"]
